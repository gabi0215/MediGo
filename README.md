# MediGo
A mobile-based healthcare service that delivers prescribed medicines to users’ homes, tracks side effects, and sends medication reminders.

# default branch -> dev
```
main(배포), dev(pr전용)
```